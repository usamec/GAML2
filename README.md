# GAML2

Rewrite of GAML assembler.
Not complete yet.

To compile:

Install googletest.
Install protocol buffers.

Then.

First time:

```
mkdir build
cd build
cmake ..
```

Then:

```
cd build
make
```
